{
  "name": "thesusrooms-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent tool framework: specs + implementations that run scenarios and return logs/video.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
