# Graphics — Spec

- Each player is rendered as a capsule shape.
- Each player has a unique assigned colour.
- In the idle state the player renders in their assigned colour.
- In the walking state the player renders with a blink animation.
