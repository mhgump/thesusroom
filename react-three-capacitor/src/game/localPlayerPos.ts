// Written by Player.tsx every frame; read by GameScene.tsx for camera follow and room rendering.
// `roomId` uses the scoped `{mapInstanceId}_{localRoomId}` form once a room is entered.
export const localPlayerPos = { x: 0, z: 0, roomId: '' };
